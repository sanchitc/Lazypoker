# Repo conventions

## Plans folder

Plan files (e.g. those produced in plan mode, or longer-form design docs)
live in `plans/` at the repo root — not in `~/.claude/plans/`. When plan
mode writes to the global path, copy the final plan into `plans/` so it's
versioned with the repo.
